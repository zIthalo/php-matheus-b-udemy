<?php 
/*  MÉTODOS OU VERBOS HTTP
    - As requisições que enviamos também contém métodos, alguns deles são:
    - GET = Solicita a apresentação de um recurso (ex: vizualização de uma página. É o que mais se usa pois sempre vamos estar requisitando alguma coisa tipo, o perfil, a aba sobre, a aba conta.);

    - POST = Envios de dados ao servidor (ex: cadastro de usuário);
    (é utilizado para enviar dados e receber alguma resposta com, dado cadastrado com sucesso, senha atualizada com sucesso etc);

    Estas 3 não são tão utilizadas no desenvolvimento web, mas só quando não tem uma API envolvida na jogada. Quando tem uma API elas são utilizadas com frequência;
    - PUT = Atualização de dados;
        (atualiza registro do banco de uma só vez)
    - DELETE = remoção de dados
        (serve para deletar dados do banco)
    - PATCH = Atualização de dado específico;
        (serve para atualizar um dado do banco)
    Quando se fala de API a gente tá falando do que cada rota da API fará, e qual verbo será indicado para cada rota;
*/

?>